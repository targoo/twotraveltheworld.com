import { Button } from './Button';

export default {
  title: 'Components/Atoms/Button',
  component: Button,
};

const Template = (args) => <Button>Button</Button>;

export const Primary = Template.bind({});
Primary.args = {};
